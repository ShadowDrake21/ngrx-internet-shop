import { ProductInCartPipe } from './product-in-cart.pipe';

describe('ProductInCartPipe', () => {
  it('create an instance', () => {
    const pipe = new ProductInCartPipe();
    expect(pipe).toBeTruthy();
  });
});
